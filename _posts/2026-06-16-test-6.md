---
layout: post
title: "test 6"
date: 2026-06-16
tags: [test, working]
excerpt: "how much testing"
---

the tags are working!

Line breaks — write with single newlines, publish, check live site. Should now render as separate paragraphs. If you put a deliberate blank line between paragraphs it'll stay as one blank line — no extra spacing added.

Edit and rename — load a post, change title, publish. This time the old SHA is fetched before anything is written, which is the most likely fix. If it still fails you'll get a toast naming the file so you know exactly what to manually delete. Also open browser console (Safari → Develop → Show Web Inspector) and watch for the log lines — that'll tell us exactly what SHA was fetched and whether the delete call succeeded.
